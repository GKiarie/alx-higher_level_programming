#!/usr/bin/python3
"""Writing to a text file"""


def write_file(filename="", text=""):
    """Ftn that writes string to a text file(UTF-8)
    and returns the number of characters written
    """
    with open(filename, 'w', encoding="utf-8") as f:
        return f.write(text)
