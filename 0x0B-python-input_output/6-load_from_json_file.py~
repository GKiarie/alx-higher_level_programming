#!/usr/bin/python3
"""Java Script Object Notation"""
import json


def save_to_json_file(my_obj, filename):
    """
    Ftn that writes an object to a text file
    usin JSON representation
    """
    with open(filename, 'w', encoding="utf-8") as f:
        json.dump(my_obj, f)
